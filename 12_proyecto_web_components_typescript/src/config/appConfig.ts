const DEFAULT_LANGUAGE: string = navigator.language.slice(0, 2) || "en";

const DEFAULT_THEME: string = "light";
const THEME_KEY: string = "theme";

export { DEFAULT_LANGUAGE, DEFAULT_THEME, THEME_KEY };
